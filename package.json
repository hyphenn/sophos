{
  "name": "sophos",
  "version": "1.0.0",
  "description": "",
  "main": "node server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "export NODE_ENV=development && node server.js",
    "dev": "export NODE_ENV=development && node server.js",
    "stg": "export NODE_ENV=staging && node server.js",
    "prod": "export NODE_ENV=staging && node server.js"
  },
  "author": "Igor BRODZIK",
  "license": "MIT",
  "dependencies": {
    "config": "^1.27.0",
    "express": "^4.16.2",
    "express-graphql": "^0.6.11",
    "graphql": "^0.11.7",
    "mongoose": "^4.12.4",
    "passport": "^0.4.0",
    "passport-jwt": "^3.0.1",
    "socket.io": "^2.0.4",
    "winston": "^2.4.0"
  }
}
